#pragma once

// Combo settings
#define COMBO_MUST_HOLD_PER_COMBO
#define COMBO_ONLY_FROM_LAYER 0
#define COMBO_TERM 50

// One-shot shift timeout (built-in QMK OSM on thumb)
#define ONESHOT_TIMEOUT 3000

// Split keyboard: sync layer state to slave half (for RGB per-layer indicators)
#define SPLIT_LAYER_STATE_ENABLE
